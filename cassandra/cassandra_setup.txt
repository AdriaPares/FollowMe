#TODO Figure out a good way of installing Cassandra.
#When everything is running, reinstall Cassandra and try to execute it without that much noise


Change files:
/etc/cassandra/cassandra.yaml
/etc/cassandra/cassandra-rackdc.properties #not sure about this one...

run cassandra cluster
sudo cassandra -R -Dcassandra.ignore_dc=true -Dcassandra.ignore_rack=true
